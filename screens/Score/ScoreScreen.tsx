import React from 'react';
import { Text } from 'react-native-paper';

import { Screen } from '../../components';

export const ScoreScreen = () => {
  return (
    <Screen testID="ScoreScreen">
      <Text>Score Screen, coming soon...</Text>
    </Screen>
  );
};
